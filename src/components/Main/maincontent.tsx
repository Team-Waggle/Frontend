import { BoardType } from "./body";

interface Prop {
  type: BoardType;
}

const MainContent = ({ type }: Prop) => {
  

    return(
        <div>
            
        </div>
    )
};

export default MainContent;
